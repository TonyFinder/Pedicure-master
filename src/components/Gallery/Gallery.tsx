import {StandardImageList} from '../../material_UI/StandardImageList/StandardImageList';

export const Gallery = () => {
    return (
        <div style={{margin: '40px 0'}}>
            <StandardImageList/>
        </div>
    )
}